enum ResourceType {
    kResourceTypeAmiga,
    kResourceTypeDOS,
    kResourceTypeMac,
}

enum Language {
    LANG_FR,
    LANG_EN,
    LANG_DE,
    LANG_SP,
    LANG_IT,
    LANG_JP,
}

enum WidescreenMode {
    kWidescreenNone,
    kWidescreenAdjacentRooms,
    kWidescreenMirrorRoom,
    kWidescreenBlur,
}

export { ResourceType, Language, WidescreenMode }